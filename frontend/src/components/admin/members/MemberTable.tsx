"use client";

import Image from "next/image";
import type { Member } from "@/types/member";
export default function MemberTable({ members, onView, onEdit, onDelete }: { members: Member[]; onView: (member: Member) => void; onEdit: (member: Member) => void; onDelete: (member: Member) => void }) { if (!members.length) return <p className="rounded-xl bg-white p-10 text-center text-slate-500 shadow">No Office Bearers Available</p>; return <div className="overflow-x-auto rounded-xl bg-white shadow"><table className="min-w-[980px] w-full text-sm"><thead className="bg-slate-100 text-left"><tr>{["Photo", "Order", "Name", "Designation", "Organization", "Phone", "Email", "Actions"].map((heading) => <th key={heading} className="p-4 font-semibold">{heading}</th>)}</tr></thead><tbody>{members.map((member) => <tr key={member._id} className="border-t"><td className="p-4"><Image src={member.photo} alt="" width={44} height={44} className="h-11 w-11 rounded-full object-cover" /></td><td className="p-4 font-semibold">{member.displayOrder}</td><td className="p-4 font-semibold">{member.fullName}</td><td className="p-4">{member.designation || "—"}</td><td className="p-4">{member.organization || "—"}</td><td className="p-4">{member.phone || "—"}</td><td className="p-4">{member.email || "—"}</td><td className="p-4"><div className="flex gap-2"><button onClick={() => onView(member)} className="text-slate-700">View</button><button onClick={() => onEdit(member)} className="text-blue-700">Edit</button><button onClick={() => onDelete(member)} className="text-red-600">Delete</button></div></td></tr>)}</tbody></table></div>; }
