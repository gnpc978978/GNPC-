"use client";

import Image from "next/image";
import type { Member } from "@/types/member";
export default function MemberModal({ member, onClose }: { member: Member | null; onClose: () => void }) { if (!member) return null; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4" role="dialog" aria-modal="true"><div className="relative w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><button onClick={onClose} className="absolute right-4 top-3 text-xl">×</button><div className="relative mx-auto h-32 w-32 overflow-hidden rounded-full"><Image src={member.photo} alt={member.fullName} fill className="object-cover" /></div><h2 className="mt-5 text-center text-2xl font-bold">{member.fullName}</h2><p className="mt-1 text-center font-semibold text-blue-700">{member.designation || "Office Bearer"}</p><dl className="mt-6 space-y-3 text-sm"><div><dt className="font-semibold">Organization</dt><dd>{member.organization || "Not provided"}</dd></div><div><dt className="font-semibold">Phone</dt><dd>{member.phone || "Not provided"}</dd></div><div><dt className="font-semibold">Email</dt><dd>{member.email || "Not provided"}</dd></div></dl></div></div>; }