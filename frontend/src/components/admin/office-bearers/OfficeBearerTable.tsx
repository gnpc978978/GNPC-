"use client";

import Image from "next/image";
import type { OfficeBearer } from "@/types/officeBearer";
export default function OfficeBearerTable({ officeBearers, onView, onEdit, onDelete }: { officeBearers: OfficeBearer[]; onView: (officeBearer: OfficeBearer) => void; onEdit: (officeBearer: OfficeBearer) => void; onDelete: (officeBearer: OfficeBearer) => void }) { if (!officeBearers.length) return <p className="rounded-xl bg-white p-10 text-center text-slate-500 shadow">No Office Bearers Available</p>; return <div className="overflow-x-auto rounded-xl bg-white shadow"><table className="min-w-[980px] w-full text-sm"><thead className="bg-slate-100 text-left"><tr>{["Photo", "Order", "Name", "Designation", "Organization", "Phone", "Email", "Actions"].map((heading) => <th key={heading} className="p-4 font-semibold">{heading}</th>)}</tr></thead><tbody>{officeBearers.map((officeBearer) => <tr key={officeBearer._id} className="border-t"><td className="p-4"><Image src={officeBearer.photo} alt="" width={44} height={44} className="h-11 w-11 rounded-full object-cover" /></td><td className="p-4 font-semibold">{officeBearer.displayOrder}</td><td className="p-4 font-semibold">{officeBearer.fullName}</td><td className="p-4">{officeBearer.designation || "—"}</td><td className="p-4">{officeBearer.organization || "—"}</td><td className="p-4">{officeBearer.phone || "—"}</td><td className="p-4">{officeBearer.email || "—"}</td><td className="p-4"><div className="flex gap-2"><button onClick={() => onView(officeBearer)} className="text-slate-700">View</button><button onClick={() => onEdit(officeBearer)} className="text-blue-700">Edit</button><button onClick={() => onDelete(officeBearer)} className="text-red-600">Delete</button></div></td></tr>)}</tbody></table></div>; }
