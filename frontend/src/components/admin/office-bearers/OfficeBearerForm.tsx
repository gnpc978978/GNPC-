"use client";

import { useState } from "react";
import type { OfficeBearer, OfficeBearerFormData } from "@/types/officeBearer";

export default function OfficeBearerForm({ officeBearer, onSubmit, onCancel }: { officeBearer?: OfficeBearer | null; onSubmit: (data: OfficeBearerFormData) => Promise<void>; onCancel: () => void }) { const [form, setForm] = useState<OfficeBearerFormData>({ fullName: officeBearer?.fullName || "", email: officeBearer?.email || "", phone: officeBearer?.phone || "", designation: officeBearer?.designation || "", organization: officeBearer?.organization || "", state: officeBearer?.state || "", district: officeBearer?.district || "", displayOrder: officeBearer?.displayOrder ?? 0 }); const [saving, setSaving] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); try { await onSubmit(form); } finally { setSaving(false); } }; const input = "w-full rounded-lg border border-slate-300 p-3"; return <form onSubmit={submit} className="grid gap-4 rounded-xl bg-slate-50 p-5 md:grid-cols-2"><input required placeholder="Full Name" value={form.fullName} onChange={(e) => setForm({ ...form, fullName: e.target.value })} className={input} /><input placeholder="Email" type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className={input} /><input placeholder="Phone" value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} className={input} /><input placeholder="Designation" value={form.designation} onChange={(e) => setForm({ ...form, designation: e.target.value })} className={input} /><input placeholder="Organization" value={form.organization} onChange={(e) => setForm({ ...form, organization: e.target.value })} className={input} /><input placeholder="State (optional)" value={form.state} onChange={(e) => setForm({ ...form, state: e.target.value })} className={input} /><input placeholder="District (optional)" value={form.district} onChange={(e) => setForm({ ...form, district: e.target.value })} className={input} /><input required min={0} type="number" placeholder="Display order" value={form.displayOrder} onChange={(e) => setForm({ ...form, displayOrder: Number(e.target.value) })} className={input} /><input required={!officeBearer} type="file" accept="image/*" onChange={(e) => setForm({ ...form, photo: e.target.files?.[0] || null })} className={input} /><div className="flex gap-3 md:col-span-2"><button disabled={saving} className="rounded-lg bg-blue-700 px-5 py-2.5 font-semibold text-white disabled:opacity-60">{saving ? "Saving..." : officeBearer ? "Update Office Bearer" : "Add Office Bearer"}</button><button type="button" onClick={onCancel} className="rounded-lg border px-5 py-2.5">Cancel</button></div></form>; }
