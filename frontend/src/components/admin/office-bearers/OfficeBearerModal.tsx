"use client";

import Image from "next/image";
import type { OfficeBearer } from "@/types/officeBearer";
export default function OfficeBearerModal({ officeBearer, onClose }: { officeBearer: OfficeBearer | null; onClose: () => void }) { if (!officeBearer) return null; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4" role="dialog" aria-modal="true"><div className="relative w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><button onClick={onClose} className="absolute right-4 top-3 text-xl">×</button><div className="relative mx-auto h-32 w-32 overflow-hidden rounded-full"><Image src={officeBearer.photo} alt={officeBearer.fullName} fill className="object-cover" /></div><h2 className="mt-5 text-center text-2xl font-bold">{officeBearer.fullName}</h2><p className="mt-1 text-center font-semibold text-blue-700">{officeBearer.designation || "Office Bearer"}</p><dl className="mt-6 space-y-3 text-sm"><div><dt className="font-semibold">Organization</dt><dd>{officeBearer.organization || "Not provided"}</dd></div><div><dt className="font-semibold">Phone</dt><dd>{officeBearer.phone || "Not provided"}</dd></div><div><dt className="font-semibold">Email</dt><dd>{officeBearer.email || "Not provided"}</dd></div></dl></div></div>; }