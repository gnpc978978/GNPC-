"use client";

import { useCallback, useEffect, useState } from "react";
import { toast } from "sonner";
import MemberForm from "@/components/admin/members/MemberForm";
import MemberTable from "@/components/admin/members/MemberTable";
import { createMember, deleteMember, exportMembers, getMembers, getMembersStats, importMembers, updateMember } from "@/services/membersService";
import type { Member, MemberFormData, MembersStats, ImportSummary } from "@/types/member";

const defaultStats: MembersStats = { total: 0, active: 0, inactive: 0 };

export default function MembersPage() {
  const [members, setMembers] = useState<Member[]>([]);
  const [stats, setStats] = useState(defaultStats);
  const [editingMember, setEditingMember] = useState<Member | null>(null);
  const [search, setSearch] = useState(""); const [designation, setDesignation] = useState(""); const [status, setStatus] = useState("");
  const [page, setPage] = useState(1); const [pages, setPages] = useState(1); const [showAll, setShowAll] = useState(false);
  const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [importSummary, setImportSummary] = useState<ImportSummary | null>(null);

  const load = useCallback(async () => { try { setLoading(true); setError(""); const params = new URLSearchParams({ page: String(page), limit: String(showAll ? 10 : 5) }); if (search) params.set("search", search); if (designation) params.set("designation", designation); if (status) params.set("status", status); const [result, memberStats] = await Promise.all([getMembers(params), getMembersStats()]); setMembers(result.data); setPages(result.pagination.pages); setStats(memberStats); } catch (caughtError) { setError(caughtError instanceof Error ? caughtError.message : "Failed to load members."); } finally { setLoading(false); } }, [designation, page, search, showAll, status]);
  useEffect(() => { const timer = window.setTimeout(() => void load(), 0); return () => window.clearTimeout(timer); }, [load]);

  const handleSubmit = async (data: MemberFormData) => { try { if (editingMember) await updateMember(editingMember._id, data); else await createMember(data); toast.success(editingMember ? "GNPC Member updated." : "GNPC Member added."); setEditingMember(null); await load(); } catch (caughtError) { toast.error(caughtError instanceof Error ? caughtError.message : "Failed to save GNPC Member."); } };
  const handleDelete = async (id: string) => { if (!window.confirm("Delete this GNPC Member permanently?")) return; try { await deleteMember(id); toast.success("GNPC Member deleted."); await load(); } catch (caughtError) { toast.error(caughtError instanceof Error ? caughtError.message : "Failed to delete GNPC Member."); } };
  const handleImport = async (file?: File) => { if (!file) return; if (!/\.(xlsx|xls)$/i.test(file.name)) { toast.error("Choose an XLSX or XLS file."); return; } try { const summary = await importMembers(file); setImportSummary(summary); toast.success(`${summary.imported} members imported.`); await load(); } catch (caughtError) { toast.error(caughtError instanceof Error ? caughtError.message : "Import Members failed."); } };
  const exportFilters = new URLSearchParams(); if (search) exportFilters.set("search", search); if (designation) exportFilters.set("designation", designation); if (status) exportFilters.set("status", status);

  return <div className="space-y-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-2xl font-bold">Members</h1><p className="text-gray-500">Manage Members</p></div><div className="flex flex-wrap gap-3"><label className="cursor-pointer rounded bg-slate-700 px-4 py-2 text-white">Import Members<input type="file" accept=".xlsx,.xls,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel" className="hidden" onChange={(event) => { void handleImport(event.target.files?.[0]); event.target.value = ""; }} /></label><button onClick={() => void exportMembers(exportFilters)} className="rounded bg-emerald-600 px-4 py-2 text-white">Export Members</button></div></div><div className="grid gap-4 sm:grid-cols-3">{[["Total Members", stats.total], ["Active Members", stats.active], ["Inactive Members", stats.inactive]].map(([label, value]) => <div key={String(label)} className="rounded-lg bg-white p-5 shadow"><p className="text-sm text-gray-500">{label}</p><p className="mt-1 text-3xl font-bold">{value}</p></div>)}</div>{importSummary && <section className="rounded-lg bg-blue-50 p-4 text-sm text-blue-950"><h2 className="font-bold">Member import complete</h2><p className="mt-2">Successfully Imported: <strong>{importSummary.imported}</strong></p><p>Failed: <strong>{importSummary.failed}</strong></p>{importSummary.failedRows.length > 0 && <><h3 className="mt-3 font-semibold">Failed rows</h3><ul className="mt-1 list-disc space-y-1 pl-5">{importSummary.failedRows.map((failure) => <li key={`${failure.row}-${failure.reason}`}>Row {failure.row}: {failure.reason}</li>)}</ul></>}</section>}<MemberForm edit={Boolean(editingMember)} initialData={editingMember ? { ...editingMember, photo: editingMember.photo || null } : undefined} onSubmit={handleSubmit} onCancel={() => setEditingMember(null)} /><div className="grid gap-3 md:grid-cols-3"><input value={search} onChange={(event) => { setSearch(event.target.value); setPage(1); }} placeholder="Search Members" className="rounded border p-2" /><input value={designation} onChange={(event) => { setDesignation(event.target.value); setPage(1); }} placeholder="Filter by designation" className="rounded border p-2" /><select value={status} onChange={(event) => { setStatus(event.target.value); setPage(1); }} className="rounded border p-2"><option value="">All statuses</option><option value="active">Active</option><option value="inactive">Inactive</option></select></div>{loading ? <p className="py-10 text-center text-gray-500">Loading Members...</p> : error ? <p className="py-10 text-center text-red-600">{error}</p> : <MemberTable members={members} onEdit={setEditingMember} onDelete={handleDelete} />}<div className="flex items-center justify-between"><button onClick={() => { setShowAll(true); setPage(1); }} className="rounded border px-4 py-2">View All Members</button>{showAll && <div className="flex gap-2"><button disabled={page === 1} onClick={() => setPage(page - 1)} className="rounded border px-3 py-1 disabled:opacity-50">Previous</button><span className="px-3 py-1">{page} / {pages}</span><button disabled={page === pages} onClick={() => setPage(page + 1)} className="rounded border px-3 py-1 disabled:opacity-50">Next</button></div>}</div></div>;
}
